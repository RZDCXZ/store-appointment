# 由 PostgreSQL 最终拒绝预约时间重叠

数据层使用 PostgreSQL 与 Drizzle ORM，但员工和宠物的有效预约区间通过显式 SQL migration 建立范围排除约束，不能只依赖应用层“先查询再写入”。所有数据库结构从第一张表开始只通过 migration 演进，应用启动时不得隐式同步 schema；确定性种子与演示重置保持独立。应用层负责计算可约时段和返回友好建议，数据库负责在并发创建或改期时保证最多一个冲突安排成功；相关行为必须使用真实 PostgreSQL 集成测试验证。
