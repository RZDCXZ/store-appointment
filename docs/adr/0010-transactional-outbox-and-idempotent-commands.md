# 使用事务 outbox 与幂等业务命令

预约创建、改期、取消、核销等可重试命令要求由客户端提供幂等键，相同身份与幂等键重复提交时返回首次结果。产生通知的业务变更在同一 PostgreSQL 事务中写入通知 outbox，由 API 进程内工作器交给默认的模拟微信通道，并独立记录每次发送尝试；自动短退避重试三次后进入需人工重试，通知失败不会回滚已经成立的预约事实。演示模式允许店长推进可注入时钟以触发开始前二十四小时提醒，并记录该操作；重试必须保持顾客消息幂等。该方案防止网络重试产生重复业务，也避免在不引入 Redis 或独立消息系统的前提下丢失通知任务。
