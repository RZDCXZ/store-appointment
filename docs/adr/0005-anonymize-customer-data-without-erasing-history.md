# 顾客删除采用匿名化并保留经营事实

顾客只有在未来预约全部处理后才能删除资料；系统随后匿名化顾客姓名、手机号、宠物档案、护理注意事项和其他身份关联，但保留不含身份信息的预约历史、经营统计及“执行过删除”这一审计事实。导出提供结构化 JSON，手机号完整揭示、导出与删除均写入审计记录。审计表不提供修改或删除接口，并由数据库触发器拒绝更新与删除；只有演示重置可以重建整个数据库。该设计在展示顾客数据权利的同时，避免物理级联删除破坏预约状态历史和门店统计；它是作品集中的演示保留规则，不声明满足任何特定司法辖区的法定期限。
