# 茸光宠物洗护预约与排班系统｜设计稿生成 PRD

Status: ready-for-design

## 0. 文档用途

本文把产品规格翻译为可直接用于 UI/UX 设计、设计稿生成和设计验收的界面要求。它定义需要生成的端、页面、关键状态、交互、视觉语言、示例内容和交付顺序，不重新定义业务规则。

设计生成与评审时的事实优先级如下：

1. 领域语言与已接受架构决策：[`CONTEXT.md`](CONTEXT.md)、[`docs/adr/`](docs/adr/)
2. 完整产品规格：[`.scratch/store-appointment/spec.md`](.scratch/store-appointment/spec.md)
3. 实施范围与里程碑：[`PROJECT_PLAN.md`](PROJECT_PLAN.md)
4. 本文中的视觉与交互建议

若前三级文档彼此出现新的业务矛盾，先回到产品文档澄清；不要在设计稿中自行创造规则。本文中的色值、示例姓名、门店地址、插图风格和画布尺寸是设计基线，可在不改变业务含义的前提下微调。

## 1. 产品摘要

### 1.1 产品定义

“茸光宠物洗护”是上海一家虚构单门店的宠物洗护品牌。产品由两个真实界面端组成：

- 顾客使用的原生微信小程序。
- 员工与店长共用的响应式 Web 后台，按身份限制导航、数据和操作。

系统让顾客根据宠物、服务规格、增项、员工技能、已发布排班和现有占用获得确定可约时段；预约创建成功时立即确认并分配具体员工。后台承接履约、异常预约、排班与容量变化、通知、档案、审计和经营信息。

### 1.2 设计目标

设计稿需要同时证明五件事：

1. **顾客容易预约**：复杂规则被转译为循序渐进的选择和明确结果。
2. **员工容易行动**：打开后台即可知道下一位宠物与当前最紧急事项。
3. **店长容易判断**：容量、时间、状态和异常之间的关系一眼可见。
4. **关键事实可信**：冲突、截止时间、权限、通知失败和历史记录均有明确反馈。
5. **作品集案例完整**：界面具有品牌识别度和真实业务密度，不像通用后台模板或普通预约表单。

### 1.3 非目标

设计中不出现以下能力：

- 支付、订金、退款、储值、优惠券、积分或发票。
- 多门店、多租户、跨店员工或白标主题。
- 真实微信登录、真实订阅消息、短信、地图或第三方日历。
- 设备、房间、浴池或多阶段流水线排程。
- 套餐、次卡、等候名单、评价、投诉、收银、库存或员工提成。
- 暗色模式、多语言、多币种、医疗诊断或治疗建议。

## 2. 角色与首要任务

| 角色 | 使用端 | 首要任务 | 设计侧重点 |
| --- | --- | --- | --- |
| 顾客 | 微信小程序 | 为一只宠物找到并创建确定预约；查看、改期或取消自己的预约 | 低认知负担、逐步选择、价格与时长透明、冲突后不丢选择 |
| 员工 | 响应式 Web 后台 | 查看本人今日预约；核销、完成、标记爽约或服务终止 | 约 390px 宽度下单手可用；下一步动作突出；宠物注意事项优先 |
| 店长 | Web 后台 | 协调今日工作、预约、排班、容量异常、通知和门店资料 | 桌面信息密度、跨员工时间关系、风险优先、操作有后果预览 |
| 代码审阅者 | 两端与本地演示入口 | 快速验证关键业务和异常场景 | 演示模式、模拟能力和真实业务事实的边界明确 |

## 3. 设计原则

### 3.1 温和，但不幼稚

宠物照片、奶油底色和圆润细节提供亲和感；排班、记录与敏感操作保持专业、克制。避免大面积卡通装饰、儿童化字体、彩虹色状态或过多爪印元素。

### 3.2 先行动，后解释

员工和店长页面先显示待处理事项及主操作，再提供历史与规则说明。顾客页面先给出当前可选择内容，只在规则影响选择时解释原因。

### 3.3 时间必须可读

日期、开始与结束时间、服务时长、周转时间、截止时间和演示时间应使用不同层级呈现。日历不能只靠块的长度表达时长；关键时刻必须有文字。

### 3.4 状态与提醒分开

“已确认、已到店、已完成、已取消、已爽约、已终止”是预约状态；“迟到待处理”“待核销”“待完成”是基于时间与状态得到的行动提醒。两者视觉语法必须不同：预约状态使用标签，行动提醒使用任务卡或提示条。

### 3.5 说明限制，不伪装能力

演示身份、模拟微信消息、本地文件、演示时间和“已完成服务标价非实收金额”都要用简短文字明确标识。提示应可信、可读，但不压过主任务。

### 3.6 危险操作分级

普通保存可直接确认；取消、服务终止、爽约、资料匿名化删除和数据重置需要说明后果，其中资料删除与数据重置必须使用二次确认。破坏性操作不与主操作使用相同颜色和位置。

## 4. 平台与设计画布

| 端 | 首选生成画布 | 适配要求 | 导航 |
| --- | --- | --- | --- |
| 微信小程序 | 390 × 844；补充 375 × 812 检查 | 适配状态栏、导航栏、底部安全区与中文键盘；主要触控目标至少 44 × 44px | 底部：首页、预约记录、消息、我的 |
| 员工后台移动版 | 390 × 844 | 不产生横向滚动；主要履约操作保持在拇指可达区域 | 精简顶部栏 + 当前角色导航 |
| 员工后台桌面版 | 1280 × 900 | 与移动版共享信息架构，但可使用详情侧栏 | 后台侧边导航 |
| 店长后台桌面版 | 1440 × 1024 | 以稳定版 Chrome 桌面为主；复杂日历最低按 1280px 验收 | 工作台、预约、排班、服务、顾客、经营、系统 |

小程序设计遵守微信原生页面心智，不把它画成带浏览器导航的移动网页。Web 后台复杂管理页面不强行缩成店长移动版；只有登录、员工工作台和员工本人预约必须在约 390px 下完整可用。

## 5. 品牌与视觉系统

### 5.1 品牌性格

关键词：`温暖`、`清爽`、`细致`、`可信`、`有秩序`。

视觉意象来自柔软毛发、晨间自然光、干净织物和修剪整齐的植物，而不是宠物医院或高饱和宠物乐园。图片应表现健康、放松、被妥善护理的犬猫，不出现医疗器械、笼养焦虑或夸张泡沫。

### 5.2 色彩基线

以下为可直接用于首轮设计稿的建议 token。允许为对比度微调，语义不可互换。

| Token | 建议色值 | 用途 |
| --- | --- | --- |
| `bg-canvas` | `#F6F1E7` | 奶油色全局背景 |
| `bg-surface` | `#FFFCF7` | 卡片、表单、浮层 |
| `sage-700` | `#3F6253` | 主要按钮、选中导航、重点文字 |
| `sage-500` | `#6F8F7E` | 次级强调、图表主色 |
| `sage-100` | `#E4ECE6` | 选中背景、温和成功态 |
| `coral-600` | `#C95F50` | 需要注意的强调、品牌点睛 |
| `coral-100` | `#F9E2DC` | 温和警示背景 |
| `ink-900` | `#24312C` | 主文字 |
| `ink-600` | `#61706A` | 次级文字 |
| `line-200` | `#DDD7CB` | 分隔线与边框 |
| `info-600` | `#376E91` | 信息提示、已确认 |
| `warning-700` | `#9A5D1F` | 迟到、待处理、重试 |
| `danger-700` | `#A84444` | 取消、失败和破坏性操作 |

珊瑚色主要作为点睛与注意色，不用作大面积正文背景。状态不能只用颜色表达；必须同时显示文字或图标。

### 5.3 字体与数字

- 中文使用系统无衬线字体栈：`PingFang SC`、`Microsoft YaHei`、系统 sans-serif。
- 标题通过字重、字号和留白建立识别，不依赖另行下载的装饰字体。
- 价格、核销码、时刻和看板数字使用等宽数字特性；核销码可用更宽字距。
- 小程序正文不小于 14px 视觉等效；Web 正文以 14–16px 为主，辅助文字不小于 12px。
- 时间写为 `10:30–11:45`，日期写为 `8月13日 周四`；后台密集表格可补充年份。

### 5.4 形状、间距与层级

- 使用 4px 基础间距网格；主要间距为 8、12、16、24、32px。
- 输入框与按钮圆角 10–12px，内容卡片 14–16px，标签使用胶囊形或 8px 圆角。
- 阴影只用于浮层、抽屉和当前需要注意的悬浮卡；普通信息层级优先靠背景与边框区分。
- 小程序页面水平留白建议 16px；后台主内容区建议 24–32px。
- 后台表格保持清晰行高和固定表头，不用连续嵌套卡片制造层级。

### 5.5 图标、照片与品牌资产

- 导航与操作图标采用同一套线性图标库，图标旁保留文字；不用 emoji 代替图标。
- 宠物照片使用具有明确使用权或生成的真实感方形图片，统一自然光、浅暖背景与近景构图。
- 员工头像使用同一摄影/生成风格；排班日历仍以员工姓名为主，头像为辅。
- 品牌标志建议为“茸光”字标加一个简洁的柔光/毛发轮廓符号。首轮设计稿需同时提供横向字标和小程序头像版本。
- 不用空白矩形、文字描述或临时插画代替最终可见资产；尚未定稿的照片也应使用风格一致的设计样片。

## 6. 统一组件与语义

### 6.1 预约状态

| 状态 | 语义 | 建议表现 | 允许的主行动 |
| --- | --- | --- | --- |
| 已确认 | 已成功占用员工与宠物时间 | 蓝灰/鼠尾草浅底标签 + 对勾或日历图标 | 核销；符合截止时改期或取消 |
| 已到店 | 已核销，服务待完成 | 鼠尾草绿标签 | 完成；服务终止 |
| 已完成 | 正常履约终态 | 深绿或中性完成标签 | 查看详情 |
| 已取消 | 到店前取消终态 | 中性灰或红灰标签 | 查看详情 |
| 已爽约 | 宽限后人工确认未到店 | 琥珀/棕色标签 | 查看详情 |
| 已终止 | 到店后未完成服务 | 红棕色标签 | 查看原因 |

### 6.2 行动提醒

行动提醒用独立任务卡呈现，包含：提醒类型、预约/宠物、发生时间、逾期或剩余时间、负责人、直接行动。至少覆盖：

- 迟到待处理
- 待核销
- 待完成
- 待处理停班或临时闭店
- 通知最终失败

### 6.3 表单

- 标签始终可见，不能只依赖 placeholder。
- 必填项、格式提示和字段错误放在字段附近。
- 涉及价格或时长变化时，在保存按钮上方持续显示“变更后摘要”。
- 长流程离开或后退时保留已填内容；提交失败也保留选择。
- 日期、体重、手机号、原因等使用适合内容的控件与键盘。

### 6.4 时间与容量

- 可约时段按钮同时显示开始时间；确认区补全结束时间、服务时长和员工。
- 不可选日期给出单一主要原因：周一闭店、暂无合格员工、已约满或超出开放窗口。
- 店长日历显示营业区间、班次、休息、停班、预约和 15 分钟周转；周转可作为预约块尾部的浅纹理段，不与顾客可见服务时长混为一体。
- 待处理容量变更用斜纹或描边遮罩覆盖影响区间，并配文字图例，不只靠红色。

### 6.5 反馈与系统状态

- 成功：就地更新事实，并用短暂 toast 说明结果。
- 可重试查询错误：保留已经显示的数据，在局部显示“更新失败”和重试按钮。
- 写入失败：保留表单和选择，不显示假成功。
- 无权限：解释当前身份无法执行，不展示可绕过入口。
- 认证过期：保留可恢复上下文，提示重新登录。
- 时段冲突：保留宠物与服务选择，突出相近可用建议。

## 7. 信息架构

### 7.1 顾客小程序

| 一级入口 | 页面与能力 |
| --- | --- |
| 首页 | 门店信息、营业状态、主要服务、最近预约、开始预约 |
| 预约记录 | 未来预约、历史预约、预约详情、核销码、改期、取消 |
| 消息 | 确认、改期、取消和开始前提醒的模拟消息 |
| 我的 | 演示顾客切换、顾客资料、宠物档案、隐私同意、数据导出、资料删除 |

预约创建是跨一级入口的主流程：`宠物 → 主要服务 → 增项 → 员工偏好 → 日期与时段 → 确认 → 成功`。

### 7.2 Web 后台

店长导航固定为：

`工作台 / 预约 / 排班 / 服务 / 顾客 / 经营 / 系统`

各入口内容如下：

| 一级入口 | 二级内容 |
| --- | --- |
| 工作台 | 风险与待办、今日状态摘要、按员工日时间线 |
| 预约 | 日历、列表、代客预约、预约详情、改期、取消、内容纠正 |
| 排班 | 周负载、排班模板、14 天草稿、已发布排班、日期例外、停班、临时闭店、影响处理 |
| 服务 | 主要服务、服务规格、增项、员工账号、员工技能 |
| 顾客 | 顾客列表、宠物档案、历史门店服务记录、导出 |
| 经营 | 7/30/90 天指标、前一等长周期比较、CSV 导出 |
| 系统 | 通知任务、审计记录、演示时间、数据重置、模拟边界说明 |

员工登录后只看到与本人履约有关的精简导航：`今日工作 / 我的预约`。员工不能看到店长管理入口。

## 8. 核心流程

### 8.1 顾客创建预约

1. 选择一只未归档宠物；无宠物时先创建档案。
2. 系统根据种类和体重展示可用主要服务与确定服务规格。
3. 选择零到多个兼容增项，实时更新总价格、服务时长和技能要求。
4. 选择指定员工或“最快可约”。
5. 从未来 14 个上海自然日中选择日期与 30 分钟候选开始时间。
6. 在确认页复核宠物、服务、增项、员工、时间、价格和服务时长。
7. 提交后立即得到“已确认”预约与具体员工。
8. 如提交时发生冲突，停留在时段步骤并显示相近建议，不清空之前选择。

### 8.2 员工履约

1. 登录后先看到下一位宠物和需要立即处理的事项。
2. 打开预约，先读取护理标签、护理注意事项与历史门店服务记录。
3. 在窗口内输入六位核销码；迟到超过 15 分钟时改为填写原因后手动核销。
4. 到达计划开始时间后，可完成服务并生成结构化门店服务记录；异常时记录原因并服务终止。
5. 未到店且超过宽限后，可人工标记爽约。
6. 已有门店服务记录需要修正时，只能追加带作者和时间的说明。

### 8.3 店长处理容量变化

1. 店长创建停班、临时闭店，或修改已发布班次/休息。
2. 如影响已有预约，先展示影响摘要；确认后进入“待处理”，对应区间立即不再接受新预约。
3. 对每笔受影响预约选择：同时间换员工、改期或取消。
4. 系统提供建议，但不自动移动或取消预约。
5. 全部预约处理完毕后容量变化才正式生效；尚未完成时可撤销待处理停班。

### 8.4 顾客改期与取消

- 开始前至少 12 小时（恰好 12 小时包含在内）可自行操作。
- 改期复用日期与时段选择，原安排持续显示，直到新安排成功。
- 改期成功后显示新旧安排对比，并提示核销码已更新。
- 截止后隐藏自行操作，改为“请联系门店处理”的明确说明。
- 顾客要更换宠物或完全不同的主要服务时，引导取消后重新预约。

## 9. 小程序页面设计要求

### 9.1 页面清单

| ID | 页面/状态 | 优先级 | 设计重点 |
| --- | --- | --- | --- |
| MP-01 | 首页 | P0 | 品牌、营业信息、最近预约、主要 CTA |
| MP-02 | 服务列表与服务详情 | P1 | 种类、体型、价格、时长和适用范围清晰 |
| MP-03 | 宠物列表 | P0 | 在用/已归档分组；未来预约阻断归档 |
| MP-04 | 新建/编辑宠物 | P0 | 体重自动得到体型；照片与护理信息 |
| MP-05 | 隐私同意 | P1 | 版本、来源、同意动作清晰 |
| MP-06 | 预约步骤：选择宠物 | P0 | 单选、一只宠物、快速新增 |
| MP-07 | 预约步骤：主要服务与增项 | P0 | 实时价格/时长摘要、兼容性 |
| MP-08 | 预约步骤：员工偏好 | P0 | 指定员工与最快可约并列解释 |
| MP-09 | 预约步骤：日期与时段 | P0 | 14 日日期条、不可用原因、时段选择 |
| MP-10 | 确认预约 | P0 | 完整复核、隐私状态、提交防重复 |
| MP-11 | 预约成功 | P0 | 已确认、具体员工、后续行动 |
| MP-12 | 时段冲突与相近建议 | P0 | 保留选择；一键替换为建议时段 |
| MP-13 | 预约记录 | P0 | 未来/历史分段；状态、时间、宠物 |
| MP-14 | 预约详情与核销码 | P0 | 当前事实、六位码、操作截止说明 |
| MP-15 | 改期与取消 | P1 | 原安排持续可见；后果确认 |
| MP-16 | 消息 | P1 | 明确为模拟消息；消息与预约可互跳 |
| MP-17 | 我的 | P1 | 演示顾客、资料、隐私、数据权利 |
| MP-18 | 数据导出与资料删除 | P2 | 未来预约阻断、匿名保留说明、二次确认 |

### 9.2 首页 MP-01

页面从上到下：

1. 品牌栏：字标、当前演示顾客入口、消息未读状态。
2. 门店摘要：今日是否营业、当前时间、地址、营业时间、联系方式。
3. 主要 CTA：“为宠物预约洗护”。
4. 最近一笔未来预约：宠物照片、服务、日期时间、员工、状态和“查看详情”。
5. 三项主要服务入口，以真实宠物图片和“起价/时长范围”辅助理解。
6. 门店承诺：确定员工、价格透明、到店核销；保持简短，不写营销长文。

无未来预约时，第 4 区改为轻量空状态并引导预约；门店闭店时仍允许浏览未来日期，不把首页主 CTA 禁用。

### 9.3 宠物档案 MP-03 / MP-04

宠物卡至少显示：照片、名称、犬/猫、品种（如有）、体重、自动体型、护理标签数量、未来预约提示。编辑页字段分组为：

- 基础资料：名称、种类、当前体重。
- 更多资料：品种、性别、出生日期、毛发类型、照片。
- 护理信息：结构化护理标签、自由文本护理注意事项。

体重输入后立即显示“8.4kg · 小型”，并说明体型影响服务规格。归档操作放在页面底部危险区；存在未来预约时禁用并指出具体预约。

### 9.4 预约步骤 MP-06 至 MP-10

- 顶部使用简洁进度与当前步骤名称，移动端不展示过长的完整面包屑。
- 每一步只保留一个明确主问题；底部固定“继续”按钮和当前价格/时长摘要。
- 主要服务卡直接显示由宠物确定的规格，例如“犬基础洗护｜小型｜¥128｜60 分钟”。
- 增项勾选后摘要平滑更新；同时显示“同一员工连续完成”。
- 员工卡显示头像、姓名、可完成的本次服务和最近可约提示；“最快可约”卡解释确定员工将在选择时段后给出。
- 日期条显示 14 个自然日；周一明确写“闭店”。无时段页面区分“暂无合格员工”和“已约满”。
- 确认页按“宠物 / 服务 / 员工 / 时间 / 价格”分组；服务时长与 15 分钟周转分开，顾客只需看到“门店另预留 15 分钟交接，不计入服务时长”的次要说明。

### 9.5 预约详情 MP-14

首屏必须同时显示：

- 预约状态与下一步说明。
- 宠物、主要服务、增项、价格快照。
- 具体员工和计划时间。
- 六位核销码（仅有效状态下显示）。

核销码下显示窗口提示：`可在开始前 30 分钟至开始后 15 分钟内出示`。改期、取消根据 12 小时规则显示为可用按钮或“请联系门店”。已完成预约补充实际完成时间，但不展示门店服务记录。

### 9.6 冲突 MP-12

冲突不是通用错误页，而是时段选择页内的高优先提示：

- 标题：“刚刚有人选走了这个时段”。
- 保留当前宠物、服务、增项与员工偏好摘要。
- 列出 3–5 个相近建议，显示日期、开始时间、结束时间和员工。
- 首个建议可作为主按钮，但不能自动替换选择。
- 提供“查看其他日期”。

## 10. 员工后台页面设计要求

### 10.1 页面清单

| ID | 页面/状态 | 优先级 | 设计重点 |
| --- | --- | --- | --- |
| ST-01 | 登录 | P0 | 演示账号说明、角色明确、错误可恢复 |
| ST-02 | 我的今日预约（390px） | P0 | 下一位宠物和行动队列优先 |
| ST-03 | 本人预约详情 | P0 | 履约资料、护理信息、历史记录 |
| ST-04 | 正常核销 | P0 | 六位码输入、窗口、重复提交反馈 |
| ST-05 | 迟到处理 | P0 | 手动核销与爽约并列但风险不同 |
| ST-06 | 完成服务 | P0 | 结构化记录、选填护理标签和文字 |
| ST-07 | 服务终止 | P1 | 原因必填、后果清晰 |
| ST-08 | 完整手机号揭示 | P1 | 主动触发、审计提示 |
| ST-09 | 追加更正说明 | P1 | 原记录只读；新增作者与时间 |

### 10.2 我的今日预约 ST-02

移动版从上到下：

1. 当前身份、日期、班次、演示模式标记。
2. “下一位宠物”大卡：照片、名称、服务、开始时间、倒计时/迟到时长、护理标签、主行动。
3. 行动队列：迟到待处理、待核销、待完成，按紧迫度排序，而非只按开始时间。
4. 今日时间线/列表：所有本人预约，显示状态和计划区间。
5. 已结束事项折叠区。

桌面版可以把今日列表放左侧、预约详情放右侧。移动版使用独立详情页或底部抽屉，不能把桌面表格压缩到 390px。

### 10.3 预约详情 ST-03

信息顺序服务于现场履约：

1. 当前状态、时间、主要行动。
2. 宠物照片、名称、种类、体重/体型、毛发类型。
3. 高优先护理标签和护理注意事项。
4. 顾客姓名、脱敏手机号与“揭示完整号码”。
5. 本次服务、增项、计划时长。
6. 该宠物历史门店服务记录。
7. 状态与变更历史。

完整手机号揭示前使用确认浮层，写明“此次访问会记录在审计中”。

### 10.4 核销、迟到与爽约 ST-04 / ST-05

- 正常核销使用六个清晰输入位或支持整串粘贴的单输入，显示预约宠物和有效窗口。
- 窗口前不提供绕过按钮，只说明何时可核销。
- 超过开始后 15 分钟时显示“手动核销”和“标记爽约”两个行动卡。
- 手动核销与爽约都要求原因；爽约使用危险动作确认，说明将释放后续容量且不会自动处罚顾客。
- 重复提交返回原结果时，用中性提示“此预约已于 10:42 核销”，不作为错误。

### 10.5 完成与服务终止 ST-06 / ST-07

完成页默认生成结构化摘要，员工只需确认，可选护理标签和内部文字。按钮文案为“完成服务并保存记录”。早于计划开始时按钮禁用并说明原因。

服务终止是独立入口，原因必填；确认文案明确它与“已完成”和“已取消”不同。完成后都显示实际结束时间与容量释放说明，但后者不应压过现场结果。

## 11. 店长后台页面设计要求

### 11.1 页面清单

| ID | 页面/状态 | 优先级 | 设计重点 |
| --- | --- | --- | --- |
| MG-01 | 今日工作台 | P0 | 风险优先、状态摘要、员工日时间线 |
| MG-02 | 按员工日历 | P0 | 班次、休息、预约、周转与异常同屏 |
| MG-03 | 预约列表与筛选 | P1 | 状态、日期、员工、服务、顾客/宠物搜索 |
| MG-04 | 预约详情抽屉 | P0 | 当前事实、历史、角色可用行动 |
| MG-05 | 代客预约 | P1 | 最小顾客资料、线下隐私来源、同一容量校验 |
| MG-06 | 店长改期 | P0 | 原安排、新建议、原因、原子失败反馈 |
| MG-07 | 预约内容纠正 | P1 | 重算价格/时长/技能/容量；前后对比 |
| MG-08 | 排班模板与 14 天草稿 | P1 | 草稿和已发布严格区分 |
| MG-09 | 已发布排班与日期例外 | P1 | 具体日期容量可见 |
| MG-10 | 停班/临时闭店创建 | P0 | 影响预览与后果说明 |
| MG-11 | 受影响预约处理 | P0 | 逐笔换员工、改期、取消；完成进度 |
| MG-12 | 主要服务/规格/增项 | P1 | 停用而非删除；新预约生效提示 |
| MG-13 | 员工与技能 | P1 | 技能矩阵、未来预约阻断停用 |
| MG-14 | 顾客与宠物档案 | P1 | 层级资料、历史记录、敏感信息 |
| MG-15 | 通知任务 | P0 | 失败尝试、三次自动重试、人工重试 |
| MG-16 | 审计记录 | P1 | 筛选、只读、操作者/时间/对象/变化 |
| MG-17 | 经营看板 | P0 | 指标公式、前期比较、非实收说明 |
| MG-18 | 演示时间与数据重置 | P1 | 演示边界、推进时间、二次确认 |

### 11.2 今日工作台 MG-01

桌面布局建议：固定侧栏 + 顶部上下文栏 + 主内容两列。

首屏从上到下、从左到右的优先级：

1. 演示模式横条：当前 `DEMO_NOW`、上海业务时间、推进时间入口。
2. 风险任务区：待处理停班、通知最终失败、迟到待处理。使用大号数字和直接行动，不先放经营图表。
3. 今日状态摘要：已确认、已到店、已完成、已取消、已爽约、已终止。
4. 按员工分列的今日时间线，可快速打开预约详情。
5. 后续事项：待核销、待完成与今日剩余容量摘要。

风险任务为空时收起为一条“当前无高优先事项”，不保留大片空白。长期经营指标不进入首页首屏。

### 11.3 按员工日历 MG-02

- 横轴为四名员工列，纵轴为 09:30–19:00 时间；支持切换日期。
- 员工列头显示头像、姓名、当日班次和负载摘要。
- 非班次区、休息、停班、临时闭店各有稳定图例。
- 预约块显示开始–结束、宠物、主要服务、状态；较短块用 tooltip/详情抽屉补全。
- 预约块尾部显示 15 分钟周转，不将其计入顾客服务时间文字。
- 待处理容量变化覆盖相关区间，并显示受影响预约数量。
- 日历可点击查看与创建，但店长改期通过显式面板完成，不采用无确认的拖拽保存。

### 11.4 预约详情与变更 MG-04 / MG-06 / MG-07

预约详情抽屉按以下层级：当前事实、宠物与顾客、本次服务、时间与员工、门店服务记录、变更历史、通知。顶部行动由状态决定。

改期面板需同时显示：

- 原员工与原计划时间，保持只读。
- 可用建议及手动选择入口。
- 新员工、新时间、价格与时长是否变化。
- 店长改期原因（必填）。
- 提交前明确“新安排失败时原安排保持不变”。

内容纠正面板需显示体重、体型、服务规格、增项的前后对比，并在保存前给出新的价格、服务时长、技能匹配和容量结果。更换宠物或完全不同主要服务时不提供保存，改为引导取消后新建。

### 11.5 排班与影响处理 MG-08 至 MG-11

排班页面必须持续区分三种对象：

- 排班模板：每周重复规则，不产生容量。
- 14 天排班草稿：可预览、编辑，未发布不产生容量。
- 已发布排班：顾客真正可预约的具体日期容量。

受影响预约处理页使用“容量变更摘要 + 完成进度 + 预约处理列表”的结构。每笔预约都显示宠物、顾客、服务、原员工、原时间和可选方案：

1. 同时间换员工：只显示覆盖全部技能且有连续容量的员工。
2. 改期：显示相近建议，并要求原因。
3. 取消：要求原因并预览通知。

处理完成一笔后标记结果但保留可追踪摘要。全部完成前顶部状态保持“待处理”；系统不提供“强制生效”。待处理停班可撤销，临时闭店与其他容量变化按规格允许的动作呈现。

### 11.6 服务与员工管理 MG-12 / MG-13

- 主要服务、服务规格、增项分别建模，但在详情中展示关联。
- 价格使用人民币，时长使用分钟，适用种类与体型有清晰选择。
- 保存变更时提示“仅影响新预约，已有预约保留快照”。
- 已被历史引用的服务、增项和员工只显示“停用”，不显示“删除”。
- 员工技能采用行列清晰的矩阵，能看出一个员工是否覆盖主要服务及所有增项。
- 停用员工前如有未来预约，阻止保存并跳转受影响预约列表。

### 11.7 通知 MG-15

通知列表状态为待发送、已发送、失败、需人工重试。通知详情显示：触发业务、顾客、关联预约、模拟微信通道、每次尝试时间与结果。三次自动失败后出现“人工重试”；页面同时说明通知失败不会撤销已经成立的预约。

### 11.8 经营看板 MG-17

顶部为 7/30/90 天分段切换，默认 30 天；所有指标均与前一等长周期比较。首屏指标：

- 服务工时利用率，附公式入口。
- 已完成服务标价，紧邻“非实收金额”说明。
- 取消率。
- 爽约率。
- 服务终止数量与比例。
- 90 天复访顾客占比，仅在相应定义下展示。

图表之外必须提供精确数值、趋势文字和定义；趋势不能只靠红绿。服务工时利用率图需要说明分子不含取消、爽约、终止与周转。经营页提供按筛选条件导出 CSV 的动作，并说明导出会记录在审计中。

### 11.9 演示与重置 MG-18

页面持续标识本地演示性质：模拟身份、模拟微信消息、本地文件存储和当前演示时间。推进时间动作说明会触发到期提醒并写入审计。

数据重置使用两步确认：

1. 第一步列出将被重建的数据和“全部旧会话失效”。
2. 第二步要求输入指定确认文字或再次明确确认。

重置按钮不与普通保存并列，不使用轻描淡写的 toast 作为唯一反馈。

## 12. 全局状态设计矩阵

每个核心页面至少设计并注释以下状态。可以通过代表性页面建立组件变体，不要求每个页面都生成一张完整独立画布。

| 状态 | 视觉与交互要求 | 代表画布 |
| --- | --- | --- |
| 首次加载 | 使用与最终布局一致的 skeleton；不闪现空状态 | MP-01、ST-02、MG-01 |
| 局部刷新 | 保留原数据，显示小型更新指示 | MG-01、MG-02 |
| 空数据 | 说明为什么为空，并给出角色允许的下一步 | MP-03、MP-13、MG-15 |
| 可重试查询错误 | 保留已有数据；局部错误条 + 重试 | MP-13、MG-02 |
| 字段校验错误 | 错误与字段关联；顶部可有错误摘要 | MP-04、MG-05 |
| 时段冲突 | 保留选择，显示相近建议 | MP-12、MG-06 |
| 无权限 | 说明身份边界；不出现可绕过控件 | ST-03、后台直接链接 |
| 认证过期 | 明确需重新登录；避免丢失可恢复输入 | ST-01 / 会话提示 |
| 状态转换非法 | 重新拉取当前事实，并解释预约已变化 | ST-04、MG-04 |
| 提交中/重复提交 | 主按钮锁定且反馈明确；幂等返回视作原结果 | MP-10、ST-04 |
| 演示模式 | 所有端持续可识别但不过度抢眼 | MP-01、ST-02、MG-01 |

## 13. 设计样例数据

以下数据只用于保持多张设计稿内容一致，最终实现以确定性种子为准。

### 13.1 固定上下文

- 设计样例时间：`2026年8月13日 周四 10:50`，上海时间。
- 门店：茸光宠物洗护。
- 地址：上海市徐汇区暖茸路 18 号（设计占位）。
- 营业：周二至周日 09:30–19:00；周一闭店。

### 13.2 员工

- 林夏：犬基础洗护、犬造型美容及全部增项。
- 陈嘉：犬基础洗护、猫咪洗护、修甲护理、除废毛护理。
- 周宁：猫咪洗护、修甲护理、口腔清洁。
- 赵航：犬基础洗护、犬造型美容、修甲护理、口腔清洁。

### 13.3 顾客、宠物与预约

- 顾客许岚，手机号 `138****2608`；宠物“团子”，柴犬，8.4kg，小型，标签“怕吹风”。
- 顾客程墨；宠物“薄荷”，英短，4.8kg，小型，标签“对陌生犬敏感”。
- 顾客陆遥；宠物“栗子”，金毛，28.6kg，大型，标签“耳部需轻柔”。
- 预约 A：团子，犬基础洗护 + 修甲护理，赵航，11:00–12:15，¥158，已确认，待核销。
- 预约 B：薄荷，猫咪洗护，陈嘉，10:30–12:00，¥168，已到店，待完成。
- 预约 C：栗子，犬基础洗护，林夏，原定 09:30，已确认，迟到待处理。

### 13.4 异常与看板样例

- 待处理停班：林夏，周六 14:00–18:00，影响 2 笔预约。
- 通知任务：预约改期通知，自动发送 3 次失败，需人工重试。
- 看板样例：服务工时利用率 68%；已完成服务标价 ¥18,860（非实收金额）；取消率 4.8%；爽约率 1.9%；服务终止率 0.8%；90 天复访顾客占比 42%。

这些数值用于排版和信息层级，不作为实现验收结果。设计稿中的同一指标不可在不同页面使用互相矛盾的数值。

## 14. 文案与领域语言

### 14.1 必须使用的产品词汇

顾客、员工、店长、宠物、预约、主要服务、增项、服务规格、班次、排班模板、已发布排班、停班、临时闭店、护理注意事项、护理标签、门店服务记录、最快可约、可约时段、到店核销、核销码、爽约、服务终止、通知任务、审计记录。

界面不将预约写成“订单”，不将员工写成“技师”，不将店长操作写成“商家操作”，不把顾客写成“会员”，不把已完成服务标价写成“营收”。

### 14.2 语气

- 顾客端：温和、简短、提供下一步。例如：“这个时段刚被选走，之前填写的内容已为你保留。”
- 员工端：直接、行动导向。例如：“已超过正常核销窗口，请填写原因后继续。”
- 店长端：说明后果和边界。例如：“确认后该区间将立即停止接受新预约；已有 2 笔预约需要逐笔处理。”
- 错误信息避免归咎顾客或承诺虚假成功。

### 14.3 关键按钮文案

| 场景 | 主按钮 | 次按钮 |
| --- | --- | --- |
| 开始预约 | 为宠物预约洗护 | 查看服务 |
| 提交预约 | 确认预约 | 返回修改 |
| 冲突建议 | 选择这个时段 | 查看其他日期 |
| 正常核销 | 确认到店 | 返回预约 |
| 完成服务 | 完成服务并保存记录 | 暂不完成 |
| 迟到处理 | 手动核销 | 标记爽约 |
| 改期 | 确认新安排 | 保留原安排 |
| 影响处理 | 保存本笔处理结果 | 稍后处理 |
| 通知失败 | 人工重试 | 查看尝试记录 |
| 数据重置 | 确认重置演示数据 | 返回系统设置 |

## 15. 无障碍与可用性要求

- Web 后台以 WCAG 2.2 AA 为目标；正文、按钮与状态标签达到可读对比度。
- 所有后台交互可用键盘完成，焦点样式清晰，焦点顺序符合视觉顺序。
- 小程序和移动后台触控目标至少 44 × 44px，并考虑底部安全区。
- 表单标签、帮助信息和错误信息建立明确关联；错误不仅显示红色边框。
- 状态、日历图例和图表同时使用文字、图标或纹理，不只依赖颜色。
- 弹窗和抽屉打开后焦点进入其内容，关闭后返回触发控件。
- 破坏性确认应说明对象与后果，不能只写“确定吗”。
- 图表旁提供数字、定义和趋势文字；表格在窄屏转为语义卡片，不缩放成不可读内容。
- 核销码、价格、时刻等关键信息支持屏幕阅读顺序，并避免用分散字符导致错误朗读。

## 16. 设计稿生成与交付顺序

### 16.1 第一批：视觉基线与三条黄金路径

先生成以下 P0 画布，用于确认整体视觉语言：

1. MP-01 首页。
2. MP-07 服务与增项。
3. MP-09 日期与时段。
4. MP-10 确认预约。
5. MP-14 预约详情与核销码。
6. ST-02 员工今日工作台，390px。
7. ST-03 员工预约详情，390px。
8. MG-01 店长今日工作台，1440px。
9. MG-02 按员工日历，1440px。
10. MG-11 受影响预约处理，1440px。

第一批完成标准：三端像同一品牌；移动端与后台有适合各自任务的密度；状态、时间、照片、按钮和图表语言已经可复用。

### 16.2 第二批：补全流程与复杂异常

生成其余 P0/P1 页面，重点补齐：冲突建议、迟到与爽约、完成与服务终止、改期、排班草稿/发布、通知失败、经营看板、演示重置。

第二批完成标准：所有核心业务变化都有开始、确认、成功、失败和当前事实回读；没有依赖说明文档才能理解的关键操作。

### 16.3 第三批：组件、状态与响应式

补齐：

- 颜色、字体、间距、图标和图片规范。
- 按钮、输入、选择卡、状态标签、任务卡、预约块、时间轴、表格、空状态、提示条、toast、弹窗、抽屉、图表图例的变体。
- 代表性加载、空数据、错误、无权限、冲突和认证过期状态。
- ST-02、ST-03 与登录页的 390px/桌面响应式对照。

第三批完成标准：开发可从组件与状态变体中实现全部页面，不需要猜测关键状态。

## 17. 设计文件结构建议

设计文件按以下页面组织：

1. `00 Cover & Notes`
2. `01 Foundations`
3. `02 Components`
4. `10 Mini Program`
5. `20 Staff Backoffice`
6. `30 Manager Backoffice`
7. `40 States & Responsive`
8. `90 Prototype Flows`

画布命名使用：`端-编号｜页面｜状态｜尺寸`，例如：

- `MP-09｜日期与时段｜默认｜390`
- `MP-12｜时段冲突｜有建议｜390`
- `ST-02｜我的今日预约｜迟到待处理｜390`
- `MG-02｜按员工日历｜待处理停班｜1440`

设计交付需包含自动布局/约束、可复用组件变体、颜色与文字样式、交互连线和关键业务注释。业务注释说明“为什么/何时出现”，不重复画面上已经看得见的内容。

## 18. 设计验收标准

满足以下条件才可视为可用于开发的设计稿：

1. 小程序、员工后台和店长后台拥有统一品牌语言，但信息密度和导航符合各自平台。
2. 顾客可以仅凭界面完成“选择宠物至已确认”的全流程，并理解具体员工、价格、时长与时间。
3. 时段冲突不会清空既有选择，并提供清晰的相近建议。
4. 员工在 390px 宽度首屏能识别下一位宠物、迟到待处理、待核销和待完成事项。
5. 店长工作台先显示风险与行动，经营图表不抢占首要位置。
6. 日历能区分班次、休息、预约、周转、停班、临时闭店和待处理容量变化。
7. “已确认”等预约状态与“迟到待处理”等行动提醒没有混为同一种标签。
8. 改期、内容纠正和容量影响处理均有前后事实、原因、建议与失败时保留原安排的说明。
9. 顾客、员工和店长的数据与操作边界在导航和页面行动中一致；敏感手机号揭示有审计提示。
10. 已完成服务标价紧邻“非实收金额”，演示时间与模拟消息持续可识别。
11. 核心页面覆盖加载、空数据、可重试错误、无权限、字段错误和冲突等代表状态。
12. 键盘焦点、触控尺寸、对比度、状态非颜色表达和表单错误关联满足无障碍要求。
13. 所有页面使用规定领域语言，没有引入支付、多门店、会员、评价等范围外能力。
14. 可见照片、头像、品牌图形和图标均为可交付资产或明确可替换的同风格设计样片。
15. 页面、组件和交互说明足以让开发实现，无需自行猜测关键业务状态。

## 19. 设计前仍可替换的内容

以下内容尚未由产品规格锁定，不阻塞首轮设计，可先采用本文默认值并保持易替换：

- 最终品牌标志与字标细节。
- 门店虚构地址、电话和品牌简介文案。
- 顾客、宠物、员工头像与照片。
- 最终 `DEMO_NOW` 日期及确定性种子中的具体姓名、预约与看板数值。
- 最终实现使用的图标库与组件库细节。

替换这些内容时，应保持页面结构、业务状态、领域词汇和角色权限不变。
