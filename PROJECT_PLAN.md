# 茸光宠物洗护预约与排班系统｜实施计划

## 1. 计划基线

本项目实现上海虚构品牌“茸光宠物洗护”的单门店预约、排班和履约系统，作为公开源码、仅在本地运行的个人作品集项目。

实施以以下文档为约束：

- 完整工程规格：`.scratch/store-appointment/spec.md`
- 领域语言：`CONTEXT.md`
- 关键架构决策：`docs/adr/`

如本计划与完整规格、领域语言或已接受 ADR 冲突，以完整规格、领域语言和 ADR 为准。发现新的领域矛盾时先修正文档，不在代码中静默创造另一套规则。

## 2. 成果目标

项目完成时必须证明：

- 顾客能在原生微信小程序中根据宠物、服务、员工技能和排班完成确定预约。
- 员工能在响应式后台中处理本人预约、到店核销、完成、爽约和服务终止。
- 店长能管理服务、员工、排班和容量异常，并处理停班或临时闭店影响的预约。
- 数据库能在并发创建和改期时最终拒绝员工或宠物时间重叠。
- 取消、改期、核销、完成、爽约、服务终止、通知和敏感操作均可追踪。
- 固定演示时间与确定性种子能够反复产生相同的时段、状态和经营指标。
- 陌生开发者可以按照文档在本地运行、测试并理解第三方边界。

本项目不设时间盒，以成果是否满足验收为终止条件。

## 3. 范围摘要

### 3.1 顾客小程序

- 门店首页、服务说明和确定的服务规格。
- 宠物档案、照片、护理标签与护理注意事项。
- 按宠物、主要服务、增项、员工偏好、日期和时段完成预约。
- 指定员工与“最快可约”。
- 预约详情、历史、六位核销码、顾客取消与顾客改期。
- 模拟消息、隐私同意、数据导出和资料删除。
- 加载、空数据、可重试错误、无权限和字段错误状态。

### 3.2 员工后台

- 响应式“我的今日预约”。
- 下一位宠物、迟到待处理、待核销和待完成事项。
- 有效核销、迟到手动核销、完成、爽约、服务终止。
- 宠物履约资料和历史门店服务记录。
- 追加服务说明，不覆盖已存在记录。

### 3.3 店长后台

- 今日工作台、按员工分列的日历、周负载摘要和预约列表。
- 代客预约、店长改期、店长取消和预约内容纠正。
- 服务、服务规格、增项、员工账号和员工技能管理。
- 排班模板、排班草稿、已发布排班、具体日期例外。
- 停班、临时闭店和受影响预约处理。
- 顾客与宠物档案、通知任务、审计记录和数据导出。
- 服务工时利用率、已完成服务标价、取消、爽约、服务终止和复访顾客看板。
- 演示时间推进和二次确认的数据重置。

## 4. 固定业务配置

### 4.1 营业与时间

- 上海时区 `Asia/Shanghai`。
- 周二至周日 09:30–19:00 营业，周一闭店。
- 候选开始时间每三十分钟生成。
- 顾客可以预约上海本地今天至第十三天，共十四个自然日。
- 最早开始时间为当前时间加两小时后向上取整到下一候选点。
- 每笔预约在服务结束后占用十五分钟周转。
- 所有时间区间采用左闭右开语义。
- 顾客取消和改期截止为开始前十二小时，恰好十二小时仍允许。
- 核销窗口为开始前三十分钟至开始后十五分钟。

### 4.2 服务与员工

实现三个主要服务和三个增项：

- 犬基础洗护
- 犬造型美容
- 猫咪洗护
- 修甲护理
- 除废毛护理
- 口腔清洁

服务价格、时长、体型规则、四名员工技能和固定班次按完整规格实现，不在开发过程中随意替换演示数字。

### 4.3 预约状态

正常路径：

`已确认 → 已到店 → 已完成`

其他终态：

- 已取消
- 已爽约
- 已终止

迟到待处理是时间派生提醒，改期是历史事件，两者不增加长期状态。

## 5. 工程架构

### 5.1 单仓库

使用 pnpm workspace 与 Turborepo，主要应用为：

- 原生 TypeScript 微信小程序
- React 19／Vite 8 响应式后台
- Node.js 24 LTS／NestJS／Fastify REST API

共享包只承载纯 TypeScript 领域值、API 契约、配置和测试工具，不跨端共享 Web 组件。

### 5.2 服务端

服务端采用模块化单体，共享一个 PostgreSQL。按以下业务能力分隔模块：

- 身份与会话
- 顾客与宠物
- 服务目录与员工技能
- 排班与容量
- 预约与履约
- 通知
- 审计与隐私
- 经营看板
- 演示支持

API 使用 REST/OpenAPI。后台通过 SSE 接收刷新提示，重连后重新读取当前事实；SSE 事件不作为数据源。

### 5.3 数据层

- PostgreSQL 18 小版本固定到实际初始化时验证的版本。
- Drizzle ORM 负责常规 schema 和查询。
- 员工和宠物有效占用的范围排除约束使用显式 SQL migration。
- 审计表更新／删除保护使用数据库触发器。
- 所有 schema 变化只通过 migration，不在应用启动时自动同步。
- 金额保存人民币分；时刻保存 UTC；排班保存上海本地日期／时间并显式绑定业务时区。
- 原计划区间、快照、状态历史和实际占用分别保存，不能互相覆盖。

### 5.4 横切机制

- 所有“现在”由可注入时钟提供，支持 `DEMO_NOW`。
- 创建、改期、取消和核销等命令使用幂等键。
- 业务变化与通知 outbox 在同一数据库事务中提交。
- 通知由 API 进程内工作器发送，自动短退避重试三次后转人工处理。
- 后台使用安全哈希的预置密码和 HttpOnly／SameSite Cookie 会话。
- 小程序演示顾客切换后由服务端签发短期 Bearer 会话。
- 种子图片随仓库提供；上传图片保存在被忽略的本地数据目录。

## 6. 测试策略

### 6.1 主要测试接缝

主要接缝是连接真实 PostgreSQL 的 HTTP API。预约、排班、容量、状态、权限、通知、审计和看板优先从调用者可观察的接口结果验证，不为控制器、服务和仓储各维护重复 mock 测试。

纯领域测试只覆盖组合密集、适合快速枚举的确定规则：

- 体型与服务规格
- 两小时取整与十四日窗口
- 三十分钟候选点
- 十二小时截止边界
- 左闭右开区间
- 技能过滤
- 周转覆盖
- “最快可约”平局
- 看板指标公式

### 6.2 测试层次

- Vitest：领域规则、格式化、错误映射和前端纯逻辑。
- 真实 PostgreSQL／Testcontainers：排除约束、事务改期、容量释放、outbox、审计触发器和 migration。
- Nest HTTP E2E：顾客、分配员工、其他员工和店长的权限与状态行为。
- Playwright：后台工作台、日历、受影响预约处理、员工履约、通知重试和演示重置。
- `miniprogram-simulate`：小程序日期选择、预约卡片、状态标签、核销码等组件。
- `miniprogram-automator`：本地手动运行三至五条黄金流程，不作为普通 CI 必过项。
- 真机：至少一台手机检查局域网请求、键盘、滚动、安全区和主流程。

### 6.3 CI 门禁

GitHub Actions 必须运行：

- 格式检查
- Lint
- 类型检查
- 领域测试
- 小程序组件测试
- API 权限测试
- 真实 PostgreSQL 集成测试
- 后台关键 E2E
- 生产构建

不在普通 Linux CI 中强制运行微信开发者工具和真机流程。

## 7. 实施原则

- 按纵向切片交付，每个阶段结束时都保留可运行路径。
- 每个切片同时完成相关业务、迁移、API、界面、错误状态和测试，不把测试推迟到最后。
- 先保证业务正确与数据历史，再做视觉精修。
- 不用“页面隐藏”替代权限，不用“先查再写”替代数据库并发约束。
- 不为未进入首版的功能提前建立通用规则 DSL、多租户或跨行业抽象。
- 新想法记录为延后项，除非发现既定规则互相矛盾，否则不改变完成定义。

## 8. 里程碑 0：工程骨架与本地环境

### 目标

建立可启动、可迁移、可测试的单仓库，不实现虚假的业务页面。

### 工作内容

- 初始化 pnpm workspace、Turborepo 和统一 TypeScript 配置。
- 初始化原生微信小程序、后台和 API 应用。
- 配置 Docker Compose PostgreSQL、健康检查和本地环境示例。
- 建立 migration、种子、重置和 `DEMO_NOW` 命令边界。
- 建立格式、Lint、类型检查、测试和构建命令。
- 建立 GitHub Actions 基础工作流。
- 提供占位微信项目配置并忽略私有 AppID 配置。

### 验收

- 空仓库克隆后可按说明安装依赖。
- PostgreSQL 能启动并通过健康检查。
- migration、种子和重置可以独立运行。
- API 与后台能同时启动。
- 微信开发者工具可以导入小程序骨架。
- 基础 CI 全绿。

## 9. 里程碑 1：领域规则与数据库约束

### 目标

在任何复杂界面之前建立唯一、可测试的预约与容量事实。

### 工作内容

- 实现可注入时钟、上海业务日期、金额和时间区间基础类型。
- 实现体型、服务规格、增项组合、技能匹配和周转规则。
- 实现排班模板、已发布排班、休息、停班和闭店的容量计算模型。
- 实现预约状态机、原计划、快照、实际占用和追加历史。
- 建立核心 schema 与全部显式 migration。
- 建立员工和宠物范围排除约束。
- 建立幂等命令表、通知 outbox 和不可修改审计基础。
- 编写领域与真实 PostgreSQL 集成测试。

### 验收

- 所有时间与截止边界有唯一测试答案。
- 技能、排班、休息、周转、停班和闭店均能排除时段。
- 同一员工或宠物的重叠占用被数据库拒绝。
- 同一时段二十个并发创建只有一个成功。
- 非法预约状态转换全部被拒绝。
- migration 能从空数据库完整建立 schema。

## 10. 里程碑 2：最小预约双端闭环

### 目标

顾客在小程序创建预约后，店长后台立即看到同一业务事实。

### 工作内容

- 建立演示顾客会话和后台预置账号登录。
- 实现门店信息、服务目录和宠物档案最小流程。
- 实现“宠物 → 主要服务 → 增项 → 员工偏好 → 日期与时段 → 确认”。
- 实现可约时段查询、指定员工和“最快可约”。
- 实现预约创建、幂等响应、时段冲突和新建议。
- 实现店长今日工作台和基础按员工日历。
- 实现 SSE 刷新提示。
- 完成相关权限、API 和关键界面测试。

### 验收

- 顾客能创建已确认且已分配员工的预约。
- 另一顾客抢同一时段时得到冲突和可用建议。
- 店长后台无需手动刷新即可获知变化并重新拉取数据。
- 顾客、员工和店长不能越权读取预约。
- 加载、空状态、查询失败和字段错误有明确界面。

## 11. 里程碑 3：履约、变更与通知

### 目标

跑通预约从确认到完成，以及取消、改期、爽约和服务终止。

### 工作内容

- 实现预约详情、六位核销码和码轮换。
- 实现正常核销、重复核销、迟到手动核销和窗口错误。
- 实现完成、爽约、服务终止和实际占用释放。
- 实现顾客取消、顾客改期、店长改期和店长取消。
- 实现店长纠正体重、服务规格和增项的完整重校验。
- 实现结构化门店服务记录与追加更正说明。
- 实现事务 outbox、模拟微信通道、失败注入、自动重试和人工重试。
- 实现开始前二十四小时提醒和演示时间推进。

### 验收

- 状态、操作者、原因、原安排与快照全部可追踪。
- 改期失败不会丢失原安排。
- 通知失败不会回滚预约，失败任务进入工作台。
- 提前完成或服务终止按实际结束加周转释放容量。
- 顾客看不到内部门店服务记录。
- 重试命令不会产生重复状态、历史或顾客消息。

## 12. 里程碑 4：排班与容量异常闭环

### 目标

店长能够维护未来容量，并在变更影响预约时安全处理。

### 工作内容

- 实现排班模板、十四天草稿、发布和日期例外。
- 实现服务、增项、员工、技能和停用管理。
- 实现停班与临时闭店。
- 实现共用的待处理容量变更流程。
- 实现受影响预约列表与可用建议。
- 实现同时间换员工、改期、取消和撤销待处理停班。
- 完善日历、周负载摘要、搜索和筛选。

### 验收

- 未发布排班不产生可约时段。
- 待处理容量变更立即阻止新预约。
- 系统不自动移动或取消受影响预约。
- 所有受影响预约处理完毕后变更才生效。
- 任何店长操作都不能强制创建重叠。
- 停用服务或员工不破坏历史快照。

## 13. 里程碑 5：隐私、档案、审计与导出

### 目标

完成角色数据边界和顾客数据权利，不把隐私做成静态说明页。

### 工作内容

- 完善宠物资料、照片、护理标签、归档和恢复。
- 实现版本化隐私同意与代客预约线下来源。
- 实现员工最小资料视图和完整手机号揭示。
- 实现顾客 JSON 导出和预约 CSV 导出。
- 实现未来预约阻断、顾客与宠物匿名化删除。
- 完成审计覆盖和数据库不可修改保护。
- 完成本地上传校验、重置清理和种子图片恢复。
- 完成 CSRF／来源、输入、文件和敏感日志安全基线。

### 验收

- 其他员工无法读取或操作不属于自己的预约。
- 完整手机号揭示、导出、删除和权限变化均可审计。
- 有未来预约的顾客不能删除，有未来预约的宠物不能归档。
- 删除匿名化身份资料但不破坏预约历史与统计。
- 应用与数据库都拒绝修改或删除审计记录。
- 重置使全部旧会话失效。

## 14. 里程碑 6：看板、确定性演示与整体精修

### 目标

把完整业务做成稳定、清楚、可重复验证的本地产品。

### 工作内容

- 实现七／三十／九十天看板及前一等长周期比较。
- 实现服务工时利用率、已完成服务标价、取消、爽约、服务终止和复访顾客指标。
- 生成二十名顾客、约二十八只宠物、一百八十天历史和未来十四天安排。
- 固定生成待处理停班、通知失败、迟到、待核销、待完成和唯一争抢时段。
- 完成三个快捷演示顾客和后台演示账号。
- 完善所有核心页面的加载、空数据、错误、无权限和冲突状态。
- 完成员工 390px 响应式工作台和店长桌面体验。
- 按茸光品牌精修小程序与后台视觉，不保留默认组件模板感。
- 完善 README、OpenAPI、架构、测试、演示账号和第三方边界说明。
- 锁定并记录实际验证的依赖、Chrome、微信、基础库和开发者工具版本。

### 验收

- 固定种子下所有指标有唯一结果和公式说明。
- 界面不把已完成服务标价称为营收。
- 从空数据库连续重置并跑通三次，时段、状态和指标一致。
- GitHub Actions 全绿。
- 小程序黄金流程在开发者工具通过。
- 至少一台真机通过局域网和主流程检查。
- 不存在主流程阻断、越权、时间冲突、状态错误或不可重置缺陷。

## 15. 本地运行承诺

默认运行步骤为：

1. `pnpm demo:up` 启动 PostgreSQL、等待健康检查、执行 migration、重置种子并启动 API 与后台。
2. 微信开发者工具导入原生小程序，填写使用者自己的测试 AppID 后编译。

不能承诺纯终端自动打开已经登录的微信开发者工具。真机访问本地 API 时，手机和电脑连接同一局域网并使用电脑 IP；不能使用手机自身的 `localhost`。

## 16. 明确不做

- 正式上线、微信审核发布、生产域名和生产运维。
- 备份恢复、监控告警、灾难演练和公网安全设施。
- 真实微信登录、真实订阅消息、短信、地图和第三方日历。
- 真实支付、订金、退款、储值、预付卡、对账和发票。
- 多门店、多租户、白标、跨行业配置和通用 SaaS。
- H5 顾客端、Taro、uni-app 和其他小程序平台。
- 设备、房间、多阶段调度、寄养、日托和医疗业务。
- 套餐、次卡、优惠券、积分、等候名单、团购、分销和营销自动化。
- 顾客评价、投诉工单、员工提成、库存和收银。
- 爽约自动处罚和服务超时自动顺延后续预约。
- 暗色模式、多语言、多币种和跨浏览器兼容矩阵。
- 正式性能 SLO、专项压测报告。
- 当前阶段的截图、录屏和个人网站案例文章。

## 17. 最终完成定义

以下条件全部满足后，首版开发结束：

- 完整规格中的已确认用户故事和实现决策全部落地。
- 所有自动化检查通过。
- 并发争抢二十次只成功一次。
- 顾客、员工和店长权限边界通过验证。
- 空数据库连续重置并完整走通三次。
- 小程序和后台在同一数据下状态一致。
- 没有主流程阻断、越权、时间冲突、状态错误或不可重置缺陷。
- README、架构说明、领域词汇表、ADR、OpenAPI、测试说明、演示账号和第三方边界齐全。

成果完成后，再单独规划截图、录屏和个人网站案例内容；这些展示物不反向扩张当前产品范围。
