# 24 — 通知失败、重试与预约提醒

**What to build:** 让店长追踪模拟微信通知的每次发送尝试，并在自动重试失败后人工恢复，同时验证开始前二十四小时提醒。

**Blocked by:** 12 — 顾客取消与顾客改期; 17 — 店长改期与店长取消; 23 — 逐笔处理受影响预约.

**Status:** ready-for-human

- [x] MG-15 通知任务列表与详情使用可直接访问和刷新恢复的后台路由。
- [x] 预约确认、顾客／店长改期、顾客／店长取消和容量影响处理结果均通过事务 outbox 生成通知任务。
- [x] 模拟微信通道展示待发送、已发送、失败和需人工重试状态，并保存每次尝试时间与结果。
- [x] 自动发送使用短退避最多重试三次，三次失败后进入工作台风险任务而不是无限重试。
- [x] 店长可以注入可预测的模拟失败并人工重试；人工重试本身写入审计事实。
- [x] 通知失败绝不回滚已经成立的预约变化，页面明确说明业务事实仍然有效。
- [x] 同一通知即使多次尝试也只在顾客消息页形成一条消息，不产生重复对客内容。
- [x] 开始前二十四小时生成提醒；创建时已经不足二十四小时的预约不补发提醒。
- [x] API 测试覆盖业务与 outbox 原子性、三次失败、人工重试、消息幂等、进程重启恢复和提醒边界。
- [x] 路由映射记录 MG-15，Playwright 覆盖失败任务、尝试详情和人工重试。

## Comments

- 2026-08-19：完成可注入演示时钟驱动的通知工作器、短退避三次自动重试、最终失败风险、人工重试审计、可预测失败注入、进程重启回收和幂等二十四小时提醒。通知投递与预约事务解耦，多次尝试只保留一条对客消息。
- 设计稿页面 → 实际路由：`MG-15 通知任务列表` → `/manager/system/notifications`；`MG-15 通知任务详情` → `/manager/system/notifications/:notificationId`。列表和详情均为独立页面模块，已验证直接访问和浏览器刷新恢复。
- 验证：真实 PostgreSQL API 集成测试覆盖 5 个通知专项场景，并复用既有预约／容量事务 outbox 测试；管理端组件覆盖加载、空态、可重试错误、无权限、写失败和成功操作；Playwright 覆盖列表直达刷新、风险入口、尝试详情、失败注入、人工重试与风险消失。
