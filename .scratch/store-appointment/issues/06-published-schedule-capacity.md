# 06 — 已发布排班形成可预约容量

**What to build:** 让店长查看四名员工在未来十四天已经发布的班次、休息和日期例外，并让这些具体安排成为唯一可预约容量来源。

**Blocked by:** 02 — 后台演示账号与角色路由; 04 — 门店首页与服务目录.

**Status:** ready-for-human

- [x] 建立四名固定员工、员工技能、门店营业时间、班次、休息和具体日期例外的数据模型与确定性基础数据。
- [x] 只有已发布的具体日期班次形成容量；周模板或未发布草稿不能被可约时段查询使用。
- [x] MG-09 已发布排班页面使用独立后台路由，可按日期查看员工班次、休息和例外并支持刷新恢复。
- [x] 页面明确区分营业时间、员工班次、休息和无排班，不把它们合并为同一种空白状态。
- [x] 周一闭店、周日无犬造型美容员工等固定场景在数据与界面中可验证。
- [x] 员工不能访问排班管理路由或完整门店容量接口。
- [x] API 测试覆盖未发布不形成容量、休息切断容量、日期例外覆盖常规安排和十四日边界。
- [x] 页面覆盖加载、空草稿、局部刷新失败与无权限状态，并保留最近成功读取的数据。
- [x] 路由映射记录 MG-09，并验证直接访问指定日期及浏览器刷新。

## Comments

- 2026-08-17：新增显式 migration，分别建模四名员工、员工技能、营业时间、周模板、具体日期草稿／已发布排班、班次、休息与日期例外；十四日基础数据跟随 `DEMO_NOW` 的上海自然日确定性生成，连续两次重置摘要一致。
- 2026-08-17：完整门店容量 API 只读取 `published` 且拥有发布时间的具体日期安排，周模板和草稿不参与容量；休息被从班次中扣除并返回分段容量，员工身份在服务端获得 403。
- 2026-08-17：MG-09 映射为 `/manager/schedule/published?date=:date`，父入口保留查询参数后重定向；页面覆盖指定日期直达、加载、空草稿、局部刷新失败保留旧数据与无权限，history fallback 验证直接访问和刷新。
- 2026-08-17：双轴代码审查后将四名员工的固定工作日、班次和休息校准到领域规格；MG-09 只保留已发布排班路由，移除属于 ticket 21 的 MG-08 页面；日期、字号与员工展示遵循设计规范，并共享上海本地日期领域值。
- 2026-08-17：`corepack pnpm check` 通过，包括全仓格式、Lint、类型检查、88 个测试和生产构建。
